server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/volticfit
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:volticfit@gmail.com}
    password: ${MAIL_PASSWORD:pnnz onsb dvcb grse}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  frontend:
    url: ${APP_FRONTEND_URL:http://localhost:4200}

security:
  jwt:
    secret-key: ${JWT_SECRET:Y29tLnByb3llY3RvLnZvbHRpY2ZpdC5zZWNyZXQua2V5LnZlcnkuc2VjdXJlLmJhc2U2NC5kZW1v}
    token-expiration: ${JWT_EXPIRATION:3600000}
    recovery-expiration: ${JWT_RECOVERY_EXP:900000}
    recovery-expiration-minutes: 15
